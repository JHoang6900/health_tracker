// The primary purpose of index.js in the routes folder is to act as a central point for combining and exporting
// multiple route modules; simplifying the import process in the main server file (server.js).
// This can be particularly useful when you have a large number of routes or want to group related routes together.

// Example Code:

// const express = require('express');
// const router = express.Router();

// const vitalsRouter = require('./vitals');
// const patientsRouter = require('./patients');
// const usersRouter = require('./users');

// router.use('/vitals', vitalsRouter);
// router.use('/patients', patientsRouter);
// router.use('/users', usersRouter);

// module.exports = router;

